# Elisa's lexer rejects carriage returns, and the bash test harnesses must stay
# LF to run under Git Bash. Normalize all text to LF on checkout on every
# platform (Windows CI would otherwise apply CRLF and break the parser).
* text=auto eol=lf
