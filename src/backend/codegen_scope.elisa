# Stage1 backend — NAME RESOLUTION and the things an emitter asks before it emits: which
# overload a call selects, what a local's slot and declared type are, which arena a growing
# container allocates from, what type an expression yields, and how a scalar binary operator
# lowers. This file is the index; each responsibility has its own file below.
# Part of `module Backend` (see codegen.elisa).

include "./codegen_overloads.elisa"
include "./codegen_locals.elisa"
include "./codegen_growth_arena.elisa"
include "./codegen_expression_type.elisa"
include "./codegen_binary_ops.elisa"
