# The single-arm self-loop fast path still has to materialize the input value
# before the arm-local binder is initialized.
def main() -> i64:
    ticks: mutable i64 = 0
    machine over ticks for item in [0]:
        state Run
        start Run
        Run, current:
            ticks <- current + 1
            -> Run
    return ticks
