module provenance_raw_lea
target x86_64
export def raw_lea(p: uintptr) -> void abi c:
    inputs:
        p = %rdi
    clobbers:
        rax, rcx, memory
    stack:
        unchanged
    control:
        returns
    body:
        leaq 8(%rdi), %rcx
        movq (%rcx), %rax
        ret
