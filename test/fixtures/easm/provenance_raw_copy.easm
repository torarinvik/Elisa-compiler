module provenance_raw_copy
target x86_64
export def raw(p: uintptr) -> void abi c:
    inputs:
        p = %rdi
    clobbers:
        rax, rcx, memory
    stack:
        unchanged
    control:
        returns
    body:
        movq %rdi, %rcx
        movq (%rcx), %rax
        ret
