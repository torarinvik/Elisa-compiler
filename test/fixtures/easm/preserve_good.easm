module preserve_good
target x86_64
export def clobber() -> void abi c:
    preserves:
        rbx
    clobbers:
        rbx
    stack:
        unchanged
    control:
        returns
    body:
        pushq %rbx
        movq $0, %rbx
        popq %rbx
        ret
