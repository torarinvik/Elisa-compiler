module direction_rmw_good
target x86_64
export def bump(p: HostPtr[u64]) -> void abi c:
    inputs:
        p = %rdi
    clobbers:
        cc, memory
    stack:
        unchanged
    control:
        returns
    body:
        addq $1, (%rdi)
        ret
