module provenance_typed_copy
target x86_64
export def typed(p: HostPtr[u64]) -> void abi c:
    inputs:
        p = %rdi
    clobbers:
        rax, rcx, memory
    stack:
        unchanged
    control:
        returns
    body:
        movq %rdi, %rcx
        movq (%rcx), %rax
        ret
