module thunk
target x86_64
template def thunk(target: HostCallable, host_fs: HostFsSelector):
    movw %fs, %r10w
    pushq %r10
    movw host_fs, %r10w
    movw %r10w, %fs
    movabs target, %r11
    call *%r11
    popq %r10
    movw %r10w, %fs
    ret
