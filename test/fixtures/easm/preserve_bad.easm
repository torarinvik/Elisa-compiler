module preserve_bad
target x86_64
export def clobber() -> void abi c:
    preserves:
        rbx
    clobbers:
        rbx
    stack:
        unchanged
    control:
        returns
    body:
        movq $0, %rbx
        ret
