module register_rmw_bad
target x86_64
export def bump(value: u64) -> void abi c:
    inputs:
        value = %rax
    clobbers:
        rcx, cc
    stack:
        unchanged
    control:
        returns
    body:
        addq %rax, %rcx
        ret
