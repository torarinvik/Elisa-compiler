module register_zero_good
target x86_64
export def clear() -> void abi c:
    clobbers:
        rax, rbx, cc
    stack:
        unchanged
    control:
        returns
    body:
        xorq %rax, %rax
        movq %rax, %rbx
        ret
