module address_register_bad
target x86_64
export def load() -> void abi c:
    clobbers:
        rbx, memory
    stack:
        unchanged
    control:
        returns
    body:
        movq (%rax), %rbx
        ret
