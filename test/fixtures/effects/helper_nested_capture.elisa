effect Tick:
    def ping() -> i64

handler static Sink(value: i64) for Tick:
    def ping() -> i64:
        return value

def emit() -> i64 can[Tick]:
    return Tick.ping()

def main() -> i64:
    can Tick with Sink(11):
        can Tick with Sink(22):
            return emit()
