effect Writer[T]:
    def write(value: T) -> void

handler Sink() for Writer[sview, i64]:
    def write(value: sview) -> void:
        return

def main() -> void:
    can Writer[sview, i64] with Sink:
        Writer[sview, i64].write("hello")
