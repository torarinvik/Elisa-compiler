effect Tick:
    def ping() -> void

effect Other:
    def ping() -> void

handler Wrong() for Other:
    def ping() -> void:
        return

def main() -> i64:
    can Tick with Wrong():
        Tick.ping()
    return 42
