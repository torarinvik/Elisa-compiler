effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def emit_tick() -> void can[Tick]:
    Tick.ping()

def main() -> void:
    can Tick with Sink:
        emit_tick()
    emit_tick()
