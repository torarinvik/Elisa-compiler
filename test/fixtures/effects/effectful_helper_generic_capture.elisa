effect Tick:
    def ping() -> void

handler static Sink(prefix: i64 = 7) for Tick:
    def ping() -> void:
        return

def emit[T](value: T) -> void can[Tick]:
    Tick.ping()

def main() -> void:
    can Tick with Sink:
        emit(7)
