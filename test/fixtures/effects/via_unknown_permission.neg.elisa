effect Writer[T]:
    def write(value: T) -> void

handler Sink() for Writer[sview]:
    def write(value: sview) -> void can[NoSuch.Write]:
        return

def main() -> void:
    can Writer[sview] with Sink:
        Writer.write("hello")
