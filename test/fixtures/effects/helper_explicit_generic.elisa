effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def emit[T](x: T) -> void can[Tick]:
    Tick.ping()

def main() -> void:
    can Tick with Sink:
        emit[i64](7)
