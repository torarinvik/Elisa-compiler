effect Tick:
    def ping() -> void

extern make_capture() -> i64

handler static TickHandler(capture: i64) for Tick:
    def ping() -> void:
        pass

def main() -> void:
    can Tick with TickHandler(make_capture()):
        Tick.ping()
        Tick.ping()
