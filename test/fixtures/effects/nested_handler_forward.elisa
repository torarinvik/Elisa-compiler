effect Tick:
    def ping() -> void
    def flush() -> void

handler Base() for Tick:
    def flush() -> void:
        pass

handler Inner() for Tick:
    def ping() -> void:
        pass

def main() -> void:
    can Tick with Base:
        can Tick with Inner:
            Tick.ping()
            Tick.flush()
