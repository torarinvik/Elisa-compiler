effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def emit(x: i64 = 7) -> void can[Tick]:
    Tick.ping()

def main() -> void:
    can Tick with Sink:
        emit()
