effect Tick:
    def ping() -> void

def require_tick() -> void can[Tick]:
    return

def emit(value: sview) -> void can[Console.Write]:
    return

def main() -> void:
    can Tick via Console.Write:
        require_tick()
        emit("hello")
