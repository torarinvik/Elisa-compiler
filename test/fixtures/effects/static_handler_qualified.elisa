module Effects:
    effect Tick:
        def ping() -> void

handler Noop() for Effects::Tick:
    def ping() -> void:
        return

def main() -> void:
    can Effects::Tick with Noop:
        Effects::Tick.ping()
