effect Tick:
    def ping() -> void

def emit(value: sview) -> void can[Console.Write]:
    return

def main() -> void:
    can Tick with Sink:
        emit("hello")
        Tick.ping()

handler Sink() for Tick via Console.Write:
    def ping() -> void:
        return
