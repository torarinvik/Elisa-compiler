effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def emit() -> void can[Tick]:
    Tick.ping()

def main() -> void:
    can Tick with Sink:
        emit()
    __effect__emit__Sink__8()
