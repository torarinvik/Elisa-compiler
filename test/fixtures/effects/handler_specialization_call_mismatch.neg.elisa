effect Writer[T]:
    def write(value: T) -> void

handler TextSink() for Writer[sview]:
    def write(value: sview) -> void:
        return

def main() -> void:
    can Writer[sview] with TextSink:
        Writer[i64].write(1)
