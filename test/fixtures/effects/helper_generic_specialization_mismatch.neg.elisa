effect Ask[T]:
    def get() -> T

handler IntAnswer() for Ask[i64]:
    def get() -> i64:
        return 41

def ask[T]() -> T can[Ask[T]]:
    return Ask[T].get()

def main() -> i64:
    can Ask[i64] with IntAnswer:
        wrong: bool = ask[bool]()
        return 0
