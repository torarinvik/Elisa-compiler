effect Tick:
    def ping(value: i64) -> i64

handler Bad() for Tick:
    def ping(value: sview) -> i64:
        return 7

def main() -> i64:
    can Tick with Bad:
        return Tick.ping(42)
    return 0
