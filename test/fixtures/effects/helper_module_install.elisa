effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

module Util:
    def emit() -> void can[Tick]:
        Tick.ping()

    def run() -> void:
        can Tick with Sink:
            emit()

def main() -> void:
    Util::run()
