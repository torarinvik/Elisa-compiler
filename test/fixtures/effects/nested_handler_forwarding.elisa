effect Tick:
    def ping() -> void
    def flush() -> void

handler Inner() for Tick:
    def ping() -> void:
        pass

handler Outer() for Tick:
    def flush() -> void:
        pass

def main() -> void:
    can Tick with Outer:
        can Tick with Inner:
            Tick.ping()
            Tick.flush()
