effect Tick:
    def ping() -> void

handler static Sink(prefix: i64 = 7) for Tick:
    def ping() -> void:
        return

def emit_tick() -> void can[Tick]:
    Tick.ping()

def wrapper() -> void can[Tick]:
    emit_tick()

def main() -> void:
    can Tick with Sink:
        wrapper()
