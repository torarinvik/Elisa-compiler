effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def emit_tick() -> void can[Tick]:
    Tick.ping()

module Util:
    def run() -> void:
        can Tick with Sink:
            emit_tick()

def main() -> void:
    Util::run()
