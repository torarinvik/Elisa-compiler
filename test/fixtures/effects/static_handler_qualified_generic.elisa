module Effects:
    effect Writer[T]:
        def write(value: T) -> void

handler Sink() for Effects::Writer[sview]:
    def write(value: sview) -> void:
        return

def main() -> void:
    can Effects::Writer[sview] with Sink:
        Effects::Writer[sview].write("hello")
