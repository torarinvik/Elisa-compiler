effect Tick:
    def ping() -> void

handler static Sink() for Tick:
    def ping() -> void:
        return

def inner() -> void can[Tick]:
    Tick.ping()

def outer() -> void can[Tick]:
    inner()

def main() -> void:
    can Tick with Sink:
        outer()
