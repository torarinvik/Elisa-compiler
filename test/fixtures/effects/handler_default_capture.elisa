effect Tick:
    def ping() -> void

handler Sink(prefix: i64 = 7) for Tick:
    def ping() -> void:
        return

def main() -> void:
    can Tick with Sink:
        Tick.ping()
        Tick.ping()
