effect Tick:
    def ping() -> void

handler static Bad() for Tick:
    def ping() -> void:
        resume()
        return

def main() -> i64:
    can Tick with Bad:
        Tick.ping()
    return 42
