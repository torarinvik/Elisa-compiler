effect Writer[T]:
    def write(value: T) -> void

handler Sink() for Writer[sview]:
    def write(value: sview) -> void can[Console.Write]:
        return

def main() -> void:
    can Writer[sview] via Console.Write with Sink:
        Writer[sview].write("hello")
