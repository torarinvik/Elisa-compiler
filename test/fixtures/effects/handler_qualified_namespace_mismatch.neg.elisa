module A:
    effect Tick:
        def ping() -> void

module B:
    effect Tick:
        def pong() -> void

handler Noop() for A::Tick:
    def ping() -> void:
        return

def main() -> void:
    can A::Tick with Noop:
        B::Tick.ping()
