def main() -> i64:
    return add_ten(descend(7))

def descend(depth: i64) -> i64:
    if depth == 0:
        return 42
    return descend(depth - 1)

def add_ten(value: i64) -> i64:
    return value + 10
