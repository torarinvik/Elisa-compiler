# The same call with a `mutable` local: the cast keeps its write capability.
def poke(p: mutable void&) -> i32:
    return 0

def main() -> i32:
    x: mutable i64 = 2
    return poke((&x).cast[void&] can Unsafe.PointerCast)
