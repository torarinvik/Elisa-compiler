# An optional return type takes null. (An AFFINE `-> P!` does too, but it is
# indistinguishable from a plain `-> P` at this site — see return_kind_cannot_hold_null —
# so struct returns are a deliberate miss rather than a false positive.)
def f() -> i64?:
    return null
