# A declared constructor is the escape hatch: both compilers accept this unchanged.
struct Box:
    a: i32
def Box(v: i32) -> Box:
    return Box{a: v}
def main() -> i32:
    can Memory.Allocate:
        x: Box = Box(1)
        return x.a
