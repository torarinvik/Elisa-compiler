# `r <- "abc"` through a `mutable u8&` would store ONE byte, not the pointer: rejected with
# the out-parameter spelling that does what the author meant.
def f(r: mutable u8&) -> i64:
    r <- "abc"
    return 0

def main() -> i32:
    a: mutable u8 = 1
    return f(&a).i32()
