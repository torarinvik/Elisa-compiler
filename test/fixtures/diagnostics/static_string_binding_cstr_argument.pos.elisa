# A binding preserves the literal's actual `static u8&` type and is not silently widened to cstr.
def take_c(v: cstr) -> i64:
    return 1

def bad(n: i64) -> i64:
    s = "zero"
    return take_c(s)
