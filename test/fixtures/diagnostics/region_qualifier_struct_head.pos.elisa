# The same head-token rule on a plain struct type, where the sentence is the scalar one.
struct Box:
    a: i32
def main() -> i32:
    can Memory.Allocate:
        b: Box @nosuch = Box{a: 1}
        return b.a
