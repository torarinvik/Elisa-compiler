# `new[NAME]` names its allocation target. stage0 resolves the selector like any
# identifier and, when it names nothing, reports the undefined name and then the
# owner-kind rule. stage1 used to say nothing and decline the body in the backend.
def f() -> i64:
    return new[missing] 7
