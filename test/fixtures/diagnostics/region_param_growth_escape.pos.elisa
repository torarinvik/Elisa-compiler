def fill[@r](values: mutable darray[i64]& @r) -> void can[Memory.Allocate, Abort.Panic]:
    region scratch(4096):
        row: mutable darray[i64] @scratch = []
        row.push(41)
        values.push(row[0])
