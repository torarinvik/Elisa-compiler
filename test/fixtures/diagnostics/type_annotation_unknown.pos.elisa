# stage0 validates a struct's and an enum's decorator NAMES against a per-declaration
# allowlist, exactly as it validates a function's. A typo compiled in silence here and the
# request it spelled simply did nothing.
@bogus
struct Point:
    x: i32

def main() -> i32:
    p: Point = Point{x: 1}
    return p.x
