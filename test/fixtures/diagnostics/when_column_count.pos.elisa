# The control: a two-column `when` over a two-column scrutinee, and the one-column form.
def pick(a: i64, b: i64) -> i64:
    return when a, b:
        0, 1 -> 1
        _, _ -> 2

def single(n: i64) -> i64:
    return when n:
        0 -> 1
        _ -> 2
