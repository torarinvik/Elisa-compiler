struct Box:
    n: mutable i64


def step(box: lmut Box) -> i64:
    box.n <- box.n + 1
    return 99


def main() -> i64:
    e: mutable Box = Box{n: 1}
    k: mutable i64 = 2
    e, k <- e.step(), k
    return e.n + k
