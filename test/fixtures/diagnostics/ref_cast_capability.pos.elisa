# Rule R: `(&y).cast[void&]` of an immutable `y` is a readonly `void&`; a `mutable void&?`
# may be written through, so the cast cannot be stored in it.
def main() -> i32:
    y: i64 = 2
    p: mutable void&? = null
    p <- (&y).cast[void&] can Unsafe.PointerCast
    return 0 if p == null else 1
