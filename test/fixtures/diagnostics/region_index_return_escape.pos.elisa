def leak_element() -> darray[i64] can[Memory.Allocate, Abort.Panic]:
    region scratch(4096):
        row: mutable darray[i64] = []
        row.push(41)
        rows: mutable darray[darray[i64]] = []
        rows.push(row)
        return rows[0]
