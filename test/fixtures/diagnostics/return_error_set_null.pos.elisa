# The suffix appears on the `got null` sentence too.
error Bad:
    Neg

def pick(n: i64) -> i64 error[Bad]:
    can Abort.Panic:
        return null

def main() -> i32:
    can Memory.Allocate, Abort.Panic:
        return 0
