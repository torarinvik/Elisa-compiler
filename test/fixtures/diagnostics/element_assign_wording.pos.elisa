# An element write is a mutation: stage0 says "cannot assign", not `variable "element"`.
def main() -> i64:
    can Memory.Allocate, Abort.Panic:
        b: bool = true
        xs: mutable darray[i64] = []
        xs.push(1)
        xs[0] <- b
        return 0
