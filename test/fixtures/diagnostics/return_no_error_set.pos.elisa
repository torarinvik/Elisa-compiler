# A function with NO declared error set must keep the bare sentence — the guard against
# an unconditional suffix.
def plain(n: i64) -> i64:
    can Abort.Panic:
        return null

def main() -> i32:
    can Memory.Allocate, Abort.Panic:
        return 0
