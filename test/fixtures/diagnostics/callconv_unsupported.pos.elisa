# stage0 underlines the decorator NAME, not the `@` and not the arguments,
# so this is cited as `1:2-10`. stage1 carried no span here and printed a bare
# line number.
@callconv(bogus)
def f() -> void:
    pass
