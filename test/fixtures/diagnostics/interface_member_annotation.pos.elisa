# The same accept-gap one block over: stage1 skipped a protocol body token by token, so a
# decorator on an interface member vanished and the file compiled.
protocol Show:
    @owner
    def show(value: i32) -> i32

def main() -> i32:
    return 0
