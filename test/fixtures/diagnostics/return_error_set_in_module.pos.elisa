# The walk that pairs a function with its error set must recurse into modules.
error Bad:
    Neg

module Inner:
    public:
        def fetch(n: i64) -> i64 error[Bad]:
            can Abort.Panic:
                return null

def main() -> i32:
    can Memory.Allocate, Abort.Panic:
        return 0
