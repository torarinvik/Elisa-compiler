affine struct Owner:
    value: i64

def inspect(owner: Owner) -> i64:
    owner.value

def main() -> i32:
    original: Owner = Owner{value: 1}
    result: i64 = inspect(original)
    0 if result == original.value else 1
