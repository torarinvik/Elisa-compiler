# `Box()` with no arguments is the same error as `Box(1)`: stage1's rule required at least
# one argument and dropped this case.
struct Box:
    a: i64 = 1
def g() -> i64:
    b: Box = Box()
    return b.a
