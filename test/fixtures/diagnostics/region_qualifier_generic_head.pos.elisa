# stage0 spans the type's HEAD TOKEN for a region-qualifier complaint -- `dict`, not the
# qualifier and not the whole generic application.
def main() -> i32:
    can Memory.Allocate:
        d: dict[i64, i64] @nosuch = {}
        return 0
