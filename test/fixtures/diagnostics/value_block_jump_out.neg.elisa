# A loop EXPRESSION owns its loop: `break` inside it stays inside the value block.
def f(xs: darray[i64]) -> i64:
    found: i64 = for x in xs |hit: i64 = -1| -> hit:
        break x if x > 3
    return found
