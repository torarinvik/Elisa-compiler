# null as a darray literal element and as a struct literal field. Neither has a literal
# kind nor an inferred family, so every existing wall stayed silent.
struct P:
    x: i64

def f() -> void:
    xs: darray[i64] = [1, null]
    p: P = P{x: null}
    _ = xs
    _ = p
