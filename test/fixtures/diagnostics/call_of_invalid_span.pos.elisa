# A lower-case unresolvable callee keeps the value-position sentence AND the cascade;
# stage0 spans the open paren on the cascade.
def main() -> i32:
    can Memory.Allocate:
        x: i32 = widget(3)
        return x
