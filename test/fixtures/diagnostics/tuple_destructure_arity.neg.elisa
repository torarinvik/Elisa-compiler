struct Box:
    n: mutable i64


def step(box: lmut Box) -> i64:
    box.n <- box.n + 1
    return 99


def main() -> i64:
    e: mutable Box = Box{n: 1}
    f: mutable Box = Box{n: 5}
    e, f <- e.step(), f.step()
    return e.n + f.n
