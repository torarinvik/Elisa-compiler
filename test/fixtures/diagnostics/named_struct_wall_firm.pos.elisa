# A firm SCALAR against a declared STRUCT wall. stage0 prints the DECLARED type -- `u8`,
# `i32`, `cstr` -- never the coarse family. Measured 2026-09-19.
struct P:
    a: i64

def f() -> void:
    can Memory.Allocate, Abort.Panic:
        u: u8 = 1
        i: i32 = 2
        d: f64 = 1.5
        c: cstr = "s"
        b: bool = true
        q1: P = u
        q2: P = i
        q3: P = d
        q4: P = c
        q5: P = b
