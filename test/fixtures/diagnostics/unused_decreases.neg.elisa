def f(n: i64) -> i64:
    decreases n
    return f(n)


# A mid-body (non-prologue) `decreases` is dropped by stage0 with no warning — only a
# leading-prologue termination clause can be "unused" (FP guard, agent-found).
def midbody(x: i64) -> i64:
    y: i64 = x
    decreases y
    return y
