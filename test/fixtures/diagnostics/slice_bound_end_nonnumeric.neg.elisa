# Valid counterpart: a numeric variable as the END bound. The end bound is not
# affected by the non-literal-start backend crash, so a darray receiver is fine here.
def f(b: i64) -> void:
    xs: mutable darray[i64] = []
    ys = xs[0:b]
    _ = ys
    _ = xs
