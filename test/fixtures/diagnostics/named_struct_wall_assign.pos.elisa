# `q <- v` against a struct wall: stage0's ASSIGN sentence (`cannot assign int to P`),
# spanning the target -- not the declaration sentence.
struct P:
    a: i64

def f() -> void:
    can Memory.Allocate, Abort.Panic:
        u: u8 = 1
        q: mutable P = P{a: 1}
        q <- 5
        q <- u
