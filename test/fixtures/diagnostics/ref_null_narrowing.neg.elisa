# Rule R + null narrowing: a writable optional reference proven non-null writes through.
def main() -> i32:
    x: mutable i64 = 1
    entry: mutable i64&? = &x
    if entry != null:
        entry <- 5
    return x.i32()
