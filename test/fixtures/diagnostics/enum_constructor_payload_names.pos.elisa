# stage0 names an enum constructor `Enum.Variant` in every one of its named-argument
# sentences; stage1 printed the bare variant for five of the six.
enum E:
    A(x: i32)

def main() -> i32:
    e: E = E.A(y: 1)
    return 0
