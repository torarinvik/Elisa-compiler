struct P:
    a: bool

def g(n: bool) -> void:
    pass

def r() -> bool:
    can Abort.Panic:
        return 5

def f() -> void:
    can Memory.Allocate, Abort.Panic:
        p: bool = 5
        xs: darray[bool] = [true, 5]
        ys: bool[2] = [true, 5]
        zs: set[bool] = {5}
        mk: dict[bool, i64] = {5: 2}
        mv: dict[i64, bool] = {1: 5}
        q: P = P{a: 5}
        g(5)
