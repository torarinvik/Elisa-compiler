# The control: the comma-list forms that ARE statements -- a tuple bind and a tuple swap.
def swap_sum() -> i64:
    a: mutable i64 = 1
    b: mutable i64 = 2
    c, d = a, b
    a, b <- b, a
    return a + b + c + d
