# The control: the same decision written with `match`, which owns computed guards, and an
# unguarded `when` beside it. Both accepted.
def classify(n: i64) -> i64:
    return match n:
        0 if n > 3: 1
        _: 2

def bucket(n: i64) -> i64:
    return when n:
        0 -> 1
        _ -> 2
