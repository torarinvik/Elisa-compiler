# Rule R: a global declared with a writable reference type must be initialised from a
# writable reference; a string literal's bytes are readonly.
global g: mutable u8& = "abc"

def main() -> i32:
    return 0
