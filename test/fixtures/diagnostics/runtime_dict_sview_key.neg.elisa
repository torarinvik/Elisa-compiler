# cstr DOES have value equality and is the spelling sview/dstr normalize to — so this is
# exactly the program a check keyed off the resolved type would have wrongly rejected.
def f(k: cstr) -> void:
    d: mutable dict[cstr, i64] = {}
    v: i64 = d[k]
    _ = v
    _ = d
