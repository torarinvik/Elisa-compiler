def copy_scalar_from_region() -> i64 can[Memory.Allocate, Abort.Panic]:
    region outer(8192):
        values: mutable darray[i64] @outer = []
        region scratch(4096):
            row: mutable darray[i64] @scratch = []
            row.push(41)
            values.push(row[0])
        return values[0]
