struct Box:
    value: i64

def safe_copy() -> Box can[Memory.Allocate, Abort.Panic]:
    region scratch(4096):
        values: mutable darray[i64] = []
        values.push(41)
        return Box{value: values[0]}
