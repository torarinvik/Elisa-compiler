def bad(flag: bool) -> i32:
    region scratch(1024) using chained
    values: mutable darray[i32] @scratch = []
    values.push(100)
    first: i32& = &values[0]
    if flag:
        values.push(1)
    result: i32 = first[0]
    destroy scratch
    return result
