# The last two null positions: a dict literal VALUE, and comparing a non-nullable value
# against null. `null` infers as no comparison group at all, so the cross-group wall
# never fired on `x == null`.
def f(x: i64) -> bool:
    d: dict[i64, i64] = {1: null}
    _ = d
    return x == null
