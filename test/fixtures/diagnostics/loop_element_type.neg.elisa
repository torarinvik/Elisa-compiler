def f() -> void:
    names: darray[cstr] = []
    for s in names:
        m: cstr = s
        _ = m
    d: dict[cstr, i64] = {}
    for k, v in d:
        total: i64 = v + 1
        _ = k
        _ = total
