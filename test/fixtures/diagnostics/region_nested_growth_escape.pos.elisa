def leak_nested() -> darray[darray[i64]] can[Memory.Allocate, Abort.Panic]:
    rows: mutable darray[darray[i64]] = []
    region scratch(4096):
        row: mutable darray[i64] = []
        row.push(41)
        rows.push(row)
    return rows
