# Declared before use, and an `is` binding used only inside the branch that bound it.
def f(o: i64?) -> i64:
    v: i64 =
        a: i64 = 20
        if o is x:
            return x + a
        c: i64 = a + 1
        c * 2
    return v
