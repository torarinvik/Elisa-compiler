# A bare `x = 100` inside a value block DECLARES a block-local shadow (stage0 lowers `=` to a
# fresh binding); the outer `x` is untouched, so this is not an E4 mutation.
def f(n: i64) -> i64:
    x: i64 = n
    y: i64 =
        x = 100
        x + 1
    return x + y
