# Elisa has no implicit positional constructor. stage0 names both ways out: the brace form,
# or a `def Box(...) -> Box` you declare yourself.
struct Box:
    a: i32
def main() -> i32:
    can Memory.Allocate:
        x: Box = Box(1)
        return 0
