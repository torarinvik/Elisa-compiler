struct P:
    a: i64

def f() -> darray[i64]:
    can Memory.Allocate, Abort.Panic:
        xs: mutable darray[i64] = []
        return xs

def g() -> darray[P]:
    can Memory.Allocate, Abort.Panic:
        ps: mutable darray[P] = []
        return ps

def h() -> darray[sview]:
    can Memory.Allocate, Abort.Panic:
        ss: mutable darray[sview] = []
        return ss
