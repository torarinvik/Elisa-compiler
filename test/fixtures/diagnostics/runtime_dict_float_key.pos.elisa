# A float dict key has no value equality, so stage0 refuses to build the hash table.
# Fires on USE; the bare declaration is accepted (see the .neg).
def f(k: f64) -> void:
    d: mutable dict[f64, i64] = {}
    d[k] <- 5
    _ = d
