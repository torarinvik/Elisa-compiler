# null passed where the parameter cannot hold it. null has no literal kind, so the
# literal-argument wall never saw it.
def g(a: i64, b: cstr) -> void:
    _ = a
    _ = b

def f() -> void:
    g(null, null)
