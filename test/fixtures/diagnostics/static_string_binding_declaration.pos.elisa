# A `static u8&` binding stored into an sview-typed declaration.
def plain(n: i64) -> i64:
    s = "zero"
    t: sview = s
    return 0

def through_block(n: i64) -> i64:
    s = "zero"
    t: sview =
        k = n
        s
    return 0
