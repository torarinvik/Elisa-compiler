# Rule R: a writable borrow of a mutable local satisfies a `mutable u8&` parameter.
def poke(r: mutable u8&) -> i32:
    r <- 7
    return 0

def main() -> i32:
    b: mutable u8 = 1
    return poke(&b)
