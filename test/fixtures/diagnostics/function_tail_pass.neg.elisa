# A body whose LAST statement is `pass` yields no value. stage1 promoted the trailing
# `pass` to an implicit `return`, which made the missing-return check terminate happily
# and left the defect to the backend; stage0 reports it.
def empty() -> i32:
    x: i32 = 1
    pass
def main() -> i32:
    return empty()
