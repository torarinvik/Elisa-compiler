# Indexing a dict yields an OPTIONAL REFERENCE (mutable i64&?), so no bare value can be
# assigned to it. stage0 rejects the form outright, whatever the value type.
def f(k: i64, v: i64) -> void:
    d: mutable dict[i64, i64] = {}
    d[k] <- v
    _ = d
