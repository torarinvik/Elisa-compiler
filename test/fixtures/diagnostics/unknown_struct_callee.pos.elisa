# stage0 reads a Capitalised callee as a struct constructor: one `unknown struct` at the
# name's extent, and no `cannot call non-function value` cascade behind it.
def main() -> i32:
    can Memory.Allocate:
        x: i32 = Widget(3)
        return x
