global mutable g1: mutable i64 = 1
global mutable g2: mutable i64 = 2

def main() -> i64:
    while g1 == 1 |g1|:
        r: mutable heap i64& = zeroed
        trusted [Unsafe.PointerCast]:
            r <- (&g2).cast[mutable heap i64&]
        g1 <- 0
    return g1 + g2
