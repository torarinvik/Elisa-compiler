package example:component@0.1.0;

interface host {
    log: func(message: string);
    notify: func(title: string) -> result<_, string>;
}

interface guest {
    start: func();
}

world app {
    import host;
    export guest;
}
