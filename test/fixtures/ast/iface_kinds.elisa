const LIMIT = 42

global mutable counter: i64

type Num = i64

using Helpers as H

module Outer:
    def inner(x: i64) -> i64:
        return x

extern strlen(s: cstr) -> usize

def visible[T](items: darray[T]&, k: i64) -> T error[Overflow]:
    return items[0]

error Overflow:
    TooBig
