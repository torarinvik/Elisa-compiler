# Regression: fixed arrays may contain fixed arrays, and indexing composes.
def main() -> i64:
    values: mutable i64[2][3] = [[1, 2], [40, 50], [60, 70]]
    values[1][0] <- 42
    return values[1][0] + values[0][1]
