# A reference binding already denotes a borrow. Re-borrowing it would create T&& and must be
# rejected before code generation.
struct Box:
    x: mutable i64

def sink(value: mutable Box&) -> void:
    value.x <- value.x + 1

def wrapper(value: mutable Box&) -> void:
    sink(&value)
