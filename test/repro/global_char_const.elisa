# A character literal is an integer code unit and must fold when used to initialize
# a typed global constant. Stage1 previously left this constant unresolved and declined
# every function that read it.
const ZERO: u8 = '0'.u8()

def main() -> i64:
    return ZERO.i64() - 48
