# A by-value scalar parameter remains the visible binding after a branch-local
# struct shadows it. The final field access must be rejected after that scope closes.
struct Counter:
    value: i64

def read(value: i64) -> i64:
    if true:
        value: Counter = Counter{value: 1}
        local: i64 = value.value
    return value.missing
