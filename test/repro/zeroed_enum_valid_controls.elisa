enum Inactive:
    Empty
    View(value: sview)
enum Nullable:
    Reference(value: i64&?)
    Empty
enum Scalar:
    Number(value: i64)
    LaterText(value: sview)

def main() -> i64:
    inactive: Inactive = zeroed
    scalar: Scalar = zeroed
    constructed: Inactive = Inactive.View("valid")
    return 42
