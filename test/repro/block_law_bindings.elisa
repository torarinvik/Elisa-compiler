# Regression fixture for block-form law binding parsing. The returned predicate
# references a binding whose RHS must be parsed and substituted, not skipped.
law NonNegative(self: i64):
    lower = self >= 0
    return lower

def main() -> i64:
    return 0
