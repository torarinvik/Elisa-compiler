# Regression: a fixed-array byte value must not be passed as a C string pointer.
# The correct spelling for the buffer address is `&text[0]`.
extern take_text(value: cstr) -> void

def main() -> void:
    text: mutable u8[4] = [53, 0, 0, 0]
    take_text(text[0])
