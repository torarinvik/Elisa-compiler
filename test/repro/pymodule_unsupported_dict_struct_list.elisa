struct BadBatch:
    labels: darray[darray[darray[darray[darray[darray[darray[darray[darray[i64]]]]]]]]]]

def shape(batches: dict[i64, darray[BadBatch]]) -> i64:
    return batches.count

export pymodule unsupported_dict_struct_list:
    fn shape
