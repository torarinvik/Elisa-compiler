struct Positive:
    value: mutable i64 where value > 0

def identity[T](value: T) -> T:
    return value

def main() -> i64:
    invalid: Positive = identity[Positive](zeroed)
    return invalid.value
