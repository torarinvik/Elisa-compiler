module Outer:
    module Types:
        type BorrowedText = sview
        type BorrowedPair = (view: BorrowedText, scalar: i64)

    module Client:
        type BorrowedText = i64

        def invalid_view() -> i64:
            pair: Types::BorrowedPair = zeroed
            return 0

def main() -> i64:
    return 42
