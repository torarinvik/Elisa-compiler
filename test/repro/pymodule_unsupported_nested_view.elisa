# A view whose element is itself an aggregate remains unsupported: the
# compositional bridge stops at scalar/text/object/named-struct view elements.
def roundtrip(values: darray[view[darray[i64]]]) -> darray[view[darray[i64]]]:
    return values

export pymodule unsupported_nested_view:
    fn roundtrip
