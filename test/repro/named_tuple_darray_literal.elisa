include "../../elisacore_std/elisacore_runtime.elisa"

def main() -> i64 can[Abort.Panic, Memory.Allocate]:
    values: darray[(first: i64, second: i64)] = [(1, 2), (3, 4)]
    return values[0].first * 100 + values[0].second * 10 + values[1].first
