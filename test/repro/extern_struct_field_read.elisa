struct State:
    victoryflag: mutable i8
    keys: mutable i16

extern gamestate: mutable State

def read_victory() -> i32:
    return gamestate.victoryflag

def read_keys(mask: i32) -> bool:
    return (gamestate.keys & mask) != 0

def main() -> i64:
    return read_victory().i64()
