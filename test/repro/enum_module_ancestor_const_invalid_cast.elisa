module Wrong:
    const enum Message of i64:
        Ready = 7
module Outer:
    module Right:
        const enum Message of i32:
            Ready = 0
    module Worker:
        def bad() -> Right::Message:
            return Wrong::Message.Ready.i32()
def main() -> i64:
    return 42
