# Regression: the manifest must reject fixed-array returns outside the C shim's bounded adapter.
def give() -> array[i64,4097]:
    return array[i64,4097]{0}

export pymodule fixed_array_return_limit:
    fn give
