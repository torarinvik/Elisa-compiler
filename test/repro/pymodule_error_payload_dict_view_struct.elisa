struct Point:
    x: i64
    label: sview

error RowsError:
    Empty(rows: dict[i64, view[Point]])

def fail(rows: dict[i64, view[Point]]) -> i64 error[RowsError]:
    raise RowsError.Empty(rows)

export pymodule error_payload_dict_view_struct:
    fn fail
