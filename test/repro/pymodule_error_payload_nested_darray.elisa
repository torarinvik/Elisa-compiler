error NestedListPayloadError:
    Empty(items: darray[darray[i64]])

def fail_nested(values: darray[darray[i64]]) -> i64 error[NestedListPayloadError]:
    raise NestedListPayloadError.Empty(values)

error StructuredNestedListPayloadError:
    Empty(items: darray[darray[i64]], label: sview)

def fail_structured(values: darray[darray[i64]], label: sview) -> i64 error[StructuredNestedListPayloadError]:
    raise StructuredNestedListPayloadError.Empty(values, label)

error DeepNestedListPayloadError:
    Empty(items: darray[darray[darray[i64]]])

def fail_deep(values: darray[darray[darray[i64]]]) -> i64 error[DeepNestedListPayloadError]:
    raise DeepNestedListPayloadError.Empty(values)

error ListSetsPayloadError:
    Empty(items: darray[set[i64]])

def fail_list_sets(values: darray[set[i64]]) -> i64 error[ListSetsPayloadError]:
    raise ListSetsPayloadError.Empty(values)

export pymodule error_payload_nested_darray:
    fn fail_nested
    fn fail_structured
    fn fail_deep
    fn fail_list_sets
