# Regression fixture for `-emit test`: a panic must preserve the diagnostic's stdout
# and the dispatch shim's active-test stderr before the child aborts.

@test
def passes() -> void:
    return

@test
def explodes() -> void can[Abort.Panic]:
    panic("boom")
