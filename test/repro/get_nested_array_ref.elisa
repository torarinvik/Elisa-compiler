struct Row:
    raw: mutable u8[16][8]

struct State:
    rows: mutable Row[2]

def bad(state: State&, index: usize, note: usize) -> i32:
    raw: u8& = get state.rows[index].raw[note] else return 0
    return raw.i32()
