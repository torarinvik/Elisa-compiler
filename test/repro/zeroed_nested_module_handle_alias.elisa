module Outer:
    module Handles:
        struct Slot:
            value: i64

        type SlotId = id[Slot]

    module Client:
        def invalid_handle() -> Outer::Handles::SlotId:
            handle: Outer::Handles::SlotId = zeroed
            return handle

def main() -> i64:
    return 42
