# A global array of niche optional references must materialize and index like stage0.
const STRINGS: u8&?[2] = ["A", "B"]

def get_string(index: u32) -> u8&?:
    return STRINGS[index.usize()]

export fn const_optional_ref_index(index: u32) -> u8&? = get_string
