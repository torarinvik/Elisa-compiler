def initialize_cstr(out: mutable cstr&) -> void:
    out <- "initialized"

def consume_cstr(value: cstr) -> i64:
    return 42

def main() -> i64:
    text: mutable cstr = zeroed
    initialize_cstr(&text)
    return consume_cstr(text)
