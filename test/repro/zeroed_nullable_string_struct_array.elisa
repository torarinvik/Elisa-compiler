struct TraceRow:
    label: mutable cstr?
    value: u64

global mutable traces: TraceRow[2] = zeroed

def main() -> i64:
    traces[0].label <- "valid"
    return 42 if traces[0].label != null else 1
