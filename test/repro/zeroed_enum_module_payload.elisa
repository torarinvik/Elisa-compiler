module Safe:
    enum Message:
        Empty
        Text(value: sview)
module Unsafe:
    type Text = sview
    enum Message:
        Text(value: Text)
        Empty
using Unsafe as U

def main() -> i64:
    message: U::Message = zeroed
    return 42
