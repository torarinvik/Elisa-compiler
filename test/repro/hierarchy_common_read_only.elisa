enum Widget layout(handle: u32):
    common:
        x: f32
        y: f32
        w: f32
        h: f32
    pass

enum Control is Widget:
    Label(text: sview)

enum Button is Control:
    Push(label: sview)

def widget_width(v: Widget) -> f32:
    return v.w

def main() -> i64:
    return 0
