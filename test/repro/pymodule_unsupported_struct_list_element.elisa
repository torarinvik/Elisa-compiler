struct BadBatch:
    labels: darray[darray[darray[darray[darray[darray[darray[darray[darray[i64]]]]]]]]]]

def shape(batches: darray[BadBatch]) -> i64:
    return batches.count

export pymodule unsupported_struct_list_element:
    fn shape
