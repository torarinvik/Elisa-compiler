struct OptionalBatch:
    signed: darray[i64?]
    narrow: darray[u8?]
    flags: darray[bool?]
    ratio: darray[f32?]
    labels: darray[cstr?]
    text: darray[sview?]

def echo(batch: OptionalBatch) -> OptionalBatch:
    return batch

export pymodule struct_optional_lists:
    fn echo
