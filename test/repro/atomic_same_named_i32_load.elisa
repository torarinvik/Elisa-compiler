struct atomic[T]:
    value: mutable T

def load(slot: atomic[i64]&, order: i32) -> i64:
    return 42

def main() -> i64:
    slot: mutable atomic[i64] = atomic[i64]{value: 0}
    order: i32 = 0
    return load(&slot, order)
