# The current Python contract identifies records by leaf name, so module-local duplicates
# must be rejected until qualified record metadata is part of the manifest.
module Left:
    struct Point:
        x: i64

module Right:
    struct Point:
        y: i64

def ping() -> i64:
    return 1

export pymodule duplicate_struct_name:
    fn ping
