# Public Python names must be unique across function and constant rows.
def first() -> i64:
    return 1

def second() -> i64:
    return 2

export pymodule duplicate_export:
    fn value = first
    fn value = second
