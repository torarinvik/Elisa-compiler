struct Point:
    x: i64
    label: sview

error RowsError:
    Empty(rows: dict[i64, dict[cstr, darray[Point]]])

def fail(rows: dict[i64, dict[cstr, darray[Point]]]) -> i64 error[RowsError]:
    raise RowsError.Empty(rows)

export pymodule error_payload_nested_named_aggregates:
    fn fail
