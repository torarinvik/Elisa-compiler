# A leading `mutable` changes the parameter binding slot, not the reference
# capability. Rebinding the readonly reference must be accepted.
def rebind(mutable p: i32&) -> i32:
    value: i32 = 7
    p <- &value
    return value

def main() -> i32:
    source: i32 = 1
    return rebind(&source) - 7
