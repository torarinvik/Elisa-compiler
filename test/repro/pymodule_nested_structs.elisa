struct Address:
    city: sview
    zip: u32

struct Profile:
    id: i64
    address: Address
    label: cstr

def roundtrip(profile: Profile) -> Profile:
    return profile

def city(profile: Profile) -> sview:
    return profile.address.city

export pymodule nested_structs:
    fn roundtrip
    fn city
