def main() -> i64:
    outer: mutable darray[i64] = []
    region scratch(4096):
        for i in 0..<200 |outer|:
            outer.push(i)
    total: mutable i64 = 0
    for v in outer |total|:
        total <- total + v
    return total % 251
