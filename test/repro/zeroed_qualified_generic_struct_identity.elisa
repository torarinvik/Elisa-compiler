module Safe:
    struct Box[T]:
        item: T?

module Borrowed:
    type Text = i64
    struct Box[T]:
        item: T

type Text = sview

def invalid() -> i64:
    box: Borrowed::Box[Text] = zeroed
    return 0

def main() -> i64:
    return 42
