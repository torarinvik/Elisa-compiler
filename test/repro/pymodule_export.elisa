# Python module export metadata smoke fixture.
def add_impl(a: i64, b: i64) -> i64:
    return a + b

def negate_impl(value: bool) -> bool:
    return not value

def ping() -> i64:
    return 42

export pymodule fastmath:
    fn add = add_impl
    fn negate = negate_impl
    fn ping
