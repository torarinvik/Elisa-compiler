struct ReferenceHolder:
    pointer: mutable i64&

def empty_holder() -> ReferenceHolder?:
    holder: ReferenceHolder? = zeroed
    return holder

def main() -> i64:
    return 42 if empty_holder() == null else 1
