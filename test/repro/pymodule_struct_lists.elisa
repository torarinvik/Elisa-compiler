struct Batch:
    values: darray[i64]
    weights: darray[f64]
    flags: darray[bool]

def shape(batch: Batch) -> i64:
    return batch.values.count + batch.weights.count + batch.flags.count

export pymodule struct_lists:
    fn shape
