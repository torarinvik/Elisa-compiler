# Regression: the manifest must reject fixed-array inputs outside the C shim's bounded adapter.
def take(values: array[i64,4097]) -> i64:
    return values[0]

export pymodule fixed_array_limit:
    fn take
