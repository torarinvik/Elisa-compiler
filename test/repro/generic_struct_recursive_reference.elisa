module Nodes:
    struct Link[T]:
        value: mutable T
        next: Link[T]&?

def main() -> i64:
    node: mutable Nodes::Link[i64] = zeroed
    node.value <- 42
    return node.value if node.next == null else 1
