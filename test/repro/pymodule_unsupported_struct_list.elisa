struct TextBatch:
    labels: darray[darray[darray[darray[darray[darray[darray[darray[darray[i64]]]]]]]]]]

def shape(batch: TextBatch) -> i64:
    return batch.labels.len

export pymodule unsupported_struct_list:
    fn shape
