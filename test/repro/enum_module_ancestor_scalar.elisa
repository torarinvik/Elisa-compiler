module Right:
    enum Message:
        Other
        Ready
module Outer:
    module Right:
        enum Message:
            Ready
            Other
    module Worker:
        def answer() -> i64:
            message: Right::Message = Right::Message.Ready
            return 42 if message is Right::Message.Ready else 1
def main() -> i64:
    return Outer::Worker::answer()
