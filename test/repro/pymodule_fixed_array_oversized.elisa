# Regression: an oversized fixed-array extent must not wrap during compiler parsing.
def too_large(values: array[i64,18446744073709551616]) -> i64:
    return values[0]

export pymodule fixed_array_oversized:
    fn too_large
