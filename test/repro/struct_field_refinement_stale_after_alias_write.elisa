struct Positive:
    value: mutable i64 where value > 0

def corrupt(target: mutable Positive&) -> Positive:
    assert target.value > 0
    alias: mutable i64& = &target.value
    alias <- 0
    return Positive{value: target.value}

def main() -> i64:
    return 0
