# nopath
include "../../elisacore_std/elisacore_runtime_prelude.elisa"

def overwrite(out: mutable cstr&) -> void:
    byte: u8 = 65.u8()
    raw: u8& = &byte
    out <- raw

def main() -> i64:
    text: mutable cstr = "safe"
    overwrite(&text)
    return text.len.i64()
