# A by-value scalar parameter may be shadowed by a struct local inside a branch.
# Its valid field access must not be mistaken for an access on the outer scalar.
struct Counter:
    value: i64

def read(value: i64) -> i64:
    if true:
        value: Counter = Counter{value: 1}
        return value.value
    return value
