# nopath
# An Arena& local supplies a valid region name while its containing region block is open.
def live() -> void:
    region owner(4096):
        alloc: mutable Arena& = (&owner).cast[mutable Arena&] can Unsafe.PointerCast
        values: mutable darray[i64] @alloc = []
