module Safe:
    struct Box[T]:
        item: mutable T

module Borrowed:
    struct Box[T]:
        view: sview

using Safe as S

def main() -> i64:
    box: mutable S::Box[i64] = zeroed
    box.item <- 42
    return box.item
