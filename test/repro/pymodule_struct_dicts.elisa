struct DictBundle:
    numbers: dict[i64, i64]
    nested: dict[cstr, darray[i64]]
    objects: dict[cstr, py::Object?]

def echo(bundle: DictBundle) -> DictBundle:
    return bundle

export pymodule struct_dicts:
    fn echo
