include "../../elisacore_std/elisacore_runtime_atomics.elisa"

# Public code must use AtomicCell helpers rather than the internal AtomicSlot API.
def forbidden_atomic_slot_access(slot: AtomicSlot[i64]&) -> i64:
    return load(slot, MemoryOrder.Acquire)

def main() -> i64:
    return 0
