struct RefHolder:
    pointer: mutable i64&

def write_through(pointer: mutable i64&) -> i64:
    pointer <- 42
    return 0

def main() -> i64:
    holder: mutable RefHolder = zeroed
    return write_through(holder.pointer)
