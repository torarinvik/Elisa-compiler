# Qualified constant targets with duplicate leaves should report a clear boundary diagnostic.
module Math:
    public:
        const VERSION: i64 = 7

module Other:
    public:
        const VERSION: i64 = 8

export pymodule qualified_constant_ambiguous:
    const version = Math::VERSION
