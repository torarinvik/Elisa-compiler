struct Positive:
    value: mutable i64 where value > 0

type PositiveAlias = Positive

struct Holder:
    inner: PositiveAlias

def main() -> i64:
    invalid: Holder = zeroed
    return invalid.inner.value
