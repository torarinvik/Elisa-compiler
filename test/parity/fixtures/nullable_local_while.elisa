struct Box:
    field: i64

def read_nested_nullable_locals(box: Box&?, local: Box&?, outer_loop: bool, inner_loop: bool) -> i64:
    if box == null:
        return 0
    while outer_loop:
        if local != null:
            while inner_loop:
                return local.field
        return box.field
    return box.field
