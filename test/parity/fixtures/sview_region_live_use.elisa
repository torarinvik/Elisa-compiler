include "../../../elisacore_std/elisacore_runtime.elisa"

def main() -> i64:
    region scratch(64)
    bytes: u8&? @scratch = new[scratch] 65
    borrowed: sview = sview(bytes, 0, 1)
    alias: sview = borrowed
    return alias.len
