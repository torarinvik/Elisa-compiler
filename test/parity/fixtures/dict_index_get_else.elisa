# Explicitly unwrap the nullable reference before using the payload as a value.
def main() -> i64:
    values: mutable dict[i64, i64] = {1: 40}
    value: i64 = get values[1] else return 0
    return value + 2
