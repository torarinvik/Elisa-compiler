# strict

struct AliasBox:
    value: mutable i64

def mutate_box(box: mutable AliasBox&):
    box.value <- 1

def main() -> i64:
    box: mutable AliasBox = AliasBox{value: 0}
    field: mutable i64& = &box.value
    mutate_box(&box)
    return 0
