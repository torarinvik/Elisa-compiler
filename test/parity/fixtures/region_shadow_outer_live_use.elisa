def main() -> i64:
    region r(64)
    outer: u8& @r = new[r] 65
    region r(64):
        inner: u8& @r = new[r] 66
        destroy r
        value: u8 = outer[0]
        if value != 65:
            return 1
    return 0
