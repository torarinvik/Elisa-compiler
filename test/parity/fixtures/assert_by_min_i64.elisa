# corpus: deliberate-decline -- stage0 proves this by WRAPPING 0 - MIN_I64; stage1 refuses
# the overflowing extremum (assert_by_min_i64_smoke.sh asserts the refusal).
def negate_min_claim(x: i64) -> void:
    requires x == -9223372036854775807 - 1
    assert 0 - x > 0 by:
        assert x == -9223372036854775807 - 1

def main() -> i64:
    return 0
