struct Node[T]:
    value: T
    next: Node[T]&?

def read_node(node: Node[i64]) -> i64:
    return node.value

def main() -> i64:
    node: Node[i64] = Node[i64]{value: 42, next: null}
    return read_node(node)
