def for_indices_par(n: usize, body: fn(usize) -> i64, w: usize = 0.usize()) -> void:
    return

def main() -> i64:
    total: usize = 4.usize()
    for i in 0.usize() ..< total by par:
        _ = i.i64()
    return 42
