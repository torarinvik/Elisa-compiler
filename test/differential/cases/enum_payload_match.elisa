enum Shape:
    Dot
    Rect(w: i64, h: i64)
def area(s: Shape) -> i64:
    match s:
        Shape.Dot:
            return 0
        Shape.Rect(w, h):
            return w * h
def main() -> i64:
    return 1 if area(Shape.Rect(3, 4)) != 12
    return 2 if area(Shape.Dot) != 0
    return 0
