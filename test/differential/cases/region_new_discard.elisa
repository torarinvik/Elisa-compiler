# A region-qualified allocation is a runtime expression even when its reference result is
# discarded. `new[r] value` must execute the copy into @r and then discard the returned reference;
# it must not be rejected merely because a statement has no expected T& type context.
def main() -> i64 can[Memory.Allocate, Abort.Panic]:
    region r(4096):
        new[r] 7
    return 0
