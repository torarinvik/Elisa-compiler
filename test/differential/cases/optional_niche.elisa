def pick(slot: mutable i64&, flag: bool) -> i64&?:
    return slot if flag
    return null
def main() -> i64:
    v: mutable i64[1] = [7]
    return 1 if pick(&v[0], false) != null
    if pick(&v[0], true) is found:
        return 2 if found[0] != 7
        return 0
    return 3
