def sum(xs: darray[i64]&) -> i64:
    return xs[0]
def main() -> i64:
    total: mutable i64 = 0
    for i in 0..<4 |total|:
        xs: mutable darray[i64] = []
        xs.push(i)
        total <- total + sum(&xs)
    return 0 if total == 6 else 1
